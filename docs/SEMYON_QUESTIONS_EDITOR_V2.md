# Вопросы по редактору v2 — эталон DESTEKER-TEXT-PECVD

Ты прислал скрин фирменного редактора из другого стека (DESTEKER). Прежде чем начну переделывать наш — уточним что реально нужно под твой VDA5050-бэк, а что визуал/фича конкретного вендора.

Стек оставляю тот же: **Vue 3 + v-network-graph + Nav2 PGM/YAML + VDA5050 LIF**. Работать буду итерациями, чтобы ты мог смотреть промежуточные результаты.

---

## 1. Basic Element — сущности для карты

На скрине левая панель, категория **Basic Element**. Ниже — как я вижу mapping на VDA5050 LIF v1.0. Отметь галочкой то, что реально нужно, зачеркни лишнее.

| DESTEKER | VDA5050 LIF | Нужно? | Комментарий |
|---|---|---|---|
| **Obstacle** | не в LIF — часть Nav2 costmap | ❓ | Рисовать поверх PGM или брать из PGM (0=obstacle)? |
| **Charging Point** | `station.stationType: "charge"` | ❓ | У нас уже есть |
| **Passing Point** | `node` без спец. типа | ❓ | Базовый waypoint |
| **Queuing Area** | несколько связанных `node`? Или `station` с несколькими `interactionNodeIds`? | ❓ | Формат неоднозначен |
| **Parking Point** | `station.stationType: "parking"` | ❓ | У нас уже есть |
| **Rotating Area** | `node.actions: [{ actionType: "rotate", ... }]` | ❓ | Или отдельная зона? |
| **Swerving Point** | `node.actions: [{ actionType: "swerve" }]`? | ❓ | Не стандартный action в VDA5050 |

**Вопросы:**
1. Все ли эти типы должны попасть в LIF-экспорт, или некоторые — только для визуализации оператора?
2. Rotating/Swerving — это `actions` на узле или отдельные зоны с полигонами?
3. Как рисовать Queuing Area — как полигон-зону (bounding box) или как цепочку узлов?

---

## 2. Business Element — вторая категория

| DESTEKER | Что это? | VDA5050? | Нужно? |
|---|---|---|---|
| **Storage Section** | стеллаж/паллета | не в LIF, оверлей карты | ❓ |
| **Auto Door** | автоматическая дверь | `node.actions: [{ actionType: "waitForDoor" }]`? | ❓ |
| **Interim Storage** | промежуточный стеллаж | не в LIF? | ❓ |
| **CTU Wait / CTU Work** | CTU-роботы, ожидание/работа | не в стандарте | ❓ |
| **Self Check** | самопроверка | `node.actions: [{ actionType: "selfCheck" }]`? | ❓ |
| **Stand By** | ожидание команды | `station.stationType: "parking"`? | ❓ |
| **Wind Door** | воздушная завеса | не в LIF? | ❓ |

**Вопросы:**
1. Business Element — это оверлей поверх Nav2-карты для оператора, или тоже должно уходить в LIF?
2. Что такое CTU? Отдельный тип робота или тип станции?
3. Хочешь чтобы каждый тип имел свой цвет/иконку/форму на канвасе?

---

## 3. Toolbar — верхняя панель

| Инструмент | Что делает | Приоритет |
|---|---|---|
| **Roam** | pan/scroll карты (без создания) | ✅ есть (нужна выделенная кнопка) |
| **Add Point** | одиночная точка | ✅ есть |
| **Draw Line** | ребро между двумя точками | ✅ есть (tool='edge') |
| **Draw Curve** | Bezier-кривая | ❓ нужна ли или прямые ок? |
| **Batch Points** | серия точек по клику | ❓ приоритет? |
| **Batch Lines** | цепочка рёбер сразу | ❓ приоритет? |
| **Align** | выровнять выбранные точки по оси | ❓ приоритет? |
| **Layout** | автолейаут (grid/tree) | ❓ приоритет? какой алгоритм? |
| **Undo/Redo** | ✅ есть | — |

**Вопросы:**
1. Draw Curve — как хранить кривую в VDA5050 LIF? Стандарт вроде поддерживает `trajectory` через контрольные точки.
2. Batch tools — критично для скорости работы или nice-to-have?
3. Align/Layout — что должно делать? Snap к сетке уже есть, что помимо?

---

## 4. Top-right — кнопки действий

| Кнопка | Что делает | Приоритет |
|---|---|---|
| **SLAM BackGround** | тумблер видимости point-cloud подложки | ❓ |
| **Export** | экспорт в файл | ✅ есть (GeoJSON + LIF) |
| **Parse** | парсинг существующего файла | ❓ Это Import? |
| **Verify** | валидация LIF/GeoJSON | ❓ Какие правила? |
| **Import Point** | загрузка точек из файла | ❓ Формат? |
| **Edit / Delete** | режим правки/удаления | ✅ есть в toolbar |
| **Download** | ??? | ❓ Отличие от Export? |
| **Save** | сохранить на бэк | ✅ есть (пока mock) |
| **Exit** | закрыть редактор | ✅ есть (Back to Maps) |

**Вопросы:**
1. Parse vs Import — это одно и то же?
2. Verify — какие правила валидации? (unreachable nodes, edges без endpoints, дубли ID?)
3. Import Point — формат файла? CSV с x/y? Или JSON-массив?

---

## 5. Общие вопросы

1. **Приоритет фич**: если ранжировать по важности для твоего бэка — что сделать первым? (1 → 5, где 1 самое важное)
   - Полный набор Basic Element типов
   - Полный набор Business Element типов
   - Batch tools (точки/линии массово)
   - Auto-layout / Align
   - SLAM BackGround toggle
   - Verify / валидация

2. **Формат вывода**: DESTEKER экспортирует в свой формат (наверное). Нам оставить VDA5050 LIF + Nav2 GeoJSON? Или тебе нужен ещё какой-то?

3. **Multi-layout**: DESTEKER показывает `DESTEKER-TEXT-PECVD` вверху — похоже на имя слоя/этажа. Multi-floor карты нужны сразу или потом?

4. **Actions**: VDA5050 у ноды может быть массив `actions` (pick/drop/charge/wait). У тебя есть предопределённый набор actions на бэке? Дай список.

---

Как ответишь по этим 5 пунктам — сяду переделывать. Пока быстрый минимум (Passing/Rotating/Charging/Parking + SLAM toggle + Roam tool) могу сделать сегодня же, если скажешь начинать.
