/**
 * Runtime-конфиг Fleet Manager.
 * Этот файл лежит рядом с index.html после сборки (в папке dist/).
 * Правь его в любом деплое БЕЗ пересборки — просто редактор + Ctrl+S + F5 в браузере.
 *
 * Приоритет полей:
 *   1. Значение в этом файле (если не пустая строка / не undefined)
 *   2. Переменные окружения из .env.local (использовались при сборке)
 *   3. Дефолты кода
 *
 * Полей три:
 *   - apiBaseUrl: адрес твоего бэка. Пример: "http://192.168.0.111:5000/api"
 *                 Пустая строка "" = использовать значение из сборки.
 *   - useMocks:   true  = локальные mock-данные (браузер не ходит на бэк вообще)
 *                 false = боевой режим, все запросы летят на apiBaseUrl
 *                 null  = использовать значение из сборки / UI-переключателя
 *   - wsUrl:      адрес WebSocket для телеметрии роботов.
 *                 Пример: "ws://192.168.0.111:5000". Пустая строка =
 *                 автоматически вычислить из apiBaseUrl (заменить http->ws, убрать /api).
 *
 * Мини-туториал для Семёна:
 *   1. Открой этот файл в блокноте
 *   2. В apiBaseUrl впиши свой URL, например "http://192.168.0.111:5000/api"
 *   3. Поставь useMocks: false
 *   4. Сохрани, обнови страницу (F5). Готово.
 *   (wsUrl обычно можно оставить пустым - фронт сам вычислит от apiBaseUrl.)
 */
window.__FLEET_CONFIG__ = {
  apiBaseUrl: '',
  useMocks: null,
  wsUrl: '',
};
